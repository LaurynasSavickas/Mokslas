let uname = 'Max';
let uage = 29;
let uhasHobbies = true;

const summarizeUser = (userName, userAge, userHobbies) =>{
    return(
        'N: ' +
        userName +
        ' A: ' + 
        userAge +
        ' H: ' +
        userHobbies
    );
};
 const add= (a,b) => a + b;
 

 console.log(add(1, 2));

console.log(summarizeUser(uname, uage, uhasHobbies ));